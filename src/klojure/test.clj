(ns klojure.test
  (:require [clojure.test :refer :all]
            [klojure.core :refer :all]))

(대응 실험함수 `deftest)
(대응 실험함수- `deftest-)
(대응 실험 `testing)
(대응 확인 `is)
(대응 확인* `are)
(정의 함수? function?)
(정의 실험-시행 run-tests)
(정의 모든-실험-시행 run-all-tests)

(대응 실험-포함 `with-test)
